fastapi
uvicorn[standard]
sqlalchemy
python-multipart
email-validator
pyjwt
bcrypt
python-dotenv

# Pinned below 1.0.0: starlette>=1.0.0 on the current index resolves to a
# release whose testclient.py imports "httpx2" (an impostor package, not the
# real httpx) instead of httpx. 0.49.3 is the last release confirmed clean.
starlette==0.49.3
